extends ../layout

block content
	.container
		.row
			form.form-horizontal(method="post", action="/admin/category")
				.form-group
					label.col-sm-1.control-label(for="inputCategory") 电影分类
					.col-sm-10
						input#inputTitle.form-control(type="text" name="name" value=category.name)
				button.btn.btn-primary(type="submit") 录入	
